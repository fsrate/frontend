{
  "routes": [
    {
      "method": "GET",
      "path": "/paypals",
      "handler": "paypal.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/paypals/count",
      "handler": "paypal.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/paypals/:id",
      "handler": "paypal.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/paypals",
      "handler": "paypal.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/paypals/:id",
      "handler": "paypal.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/paypals/:id",
      "handler": "paypal.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
